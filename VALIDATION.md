# Validation Record

## Static validation

- All included TypeScript and TSX files passed TypeScript strict checking using local dependency stubs.
- All included TypeScript and TSX files passed TypeScript syntax transpilation.
- `next.config.ts` does not suppress TypeScript build errors.

## Functional validation

### Strategic opportunity

A representative facility test confirmed:

- four modules are produced
- CMS Performance Context is not monetized
- conservative strategic value does not exceed opportunity strategic value
- available-bed context is calculated from certified beds less average residents/day
- base Paycor-influenced benefit remains separate from the strategic range

### Guided estimates

A CMS-context test confirmed:

- headcount uses resident census and total nurse HPRD when available
- the headcount estimate receives medium confidence
- Medicare Part A planning revenue can be generated transparently
- temporary Paycor pricing remains `reportable: false`

## Required deployment validation

The CMS API must still be tested after upload because the live schema and service response cannot be guaranteed by static validation. Follow `README_UPLOAD.md`.
